<div class="section section--default">
	<div class="container">
		<div class="px-10 py-10 text-white bg-primary">
			<div class="mb-5 text-xl font-bold uppercase">
				{{ block.titel }}
			</div>

			<div class="w-3/4 mb-5">
				{{ block.intro }}
			</div>

			<div>
				<a class="btn btn--ghost" href="{{ block.cta }}">{{ block.cta.getText() }}</a>
			</div>
		</div>
	</div>
</div>
